import 'package:flutter/material.dart';

class GridItem {
  final IconData icon;
  final String label;
  final String sublabel;
  final List<Color> gradient;
  const GridItem(this.icon, this.label, this.sublabel, this.gradient);
}